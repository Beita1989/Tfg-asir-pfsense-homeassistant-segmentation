# Home Assistant (Sanitized Examples)

Place *example* YAML here only (no tokens, no passwords).

Suggested files:
- configuration.example.yaml
- automations.example.yaml
- scripts.example.yaml

Tip:
Use placeholders like:
- CHANGEME_STRONG_PASSWORD
- YOUR_BROKER_IP
- YOUR_HA_IP
